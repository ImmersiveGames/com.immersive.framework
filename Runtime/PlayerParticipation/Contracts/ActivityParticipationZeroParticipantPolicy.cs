namespace Immersive.Framework.PlayerParticipation
{
    /// <summary>
    /// Explicitly declares whether a dynamic Activity projection may resolve to zero Slots.
    /// </summary>
    public enum ActivityParticipationZeroParticipantPolicy
    {
        Allowed = 0,
        Rejected = 10
    }
}
